name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  # Build the sandboxed App Store variant and upload it to App Store Connect.
  # This only delivers the build; picking it for a version and submitting for
  # review still happens in App Store Connect.
  app-store:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install tools
        run: brew install xcodegen just

      - name: Import Developer ID certificate
        env:
          CERT_P12: ${{ secrets.MACOS_CERTIFICATE_P12 }}
          CERT_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
        run: |
          KEYCHAIN="$RUNNER_TEMP/build.keychain"
          KEYCHAIN_PASSWORD=$(uuidgen)
          echo "$CERT_P12" | base64 --decode > "$RUNNER_TEMP/cert.p12"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security import "$RUNNER_TEMP/cert.p12" -k "$KEYCHAIN" \
            -P "$CERT_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S "apple-tool:,apple:" \
            -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security list-keychains -d user -s "$KEYCHAIN" login.keychain
          rm "$RUNNER_TEMP/cert.p12"

      - name: Build and upload to App Store Connect
        env:
          APP_STORE_CONNECT_API_KEY: ${{ secrets.APP_STORE_CONNECT_API_KEY }}
          APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          export APP_VERSION="${GITHUB_REF_NAME#v}"
          export APP_BUILD="$(git rev-list --count HEAD)"
          KEY_FILE="$RUNNER_TEMP/AuthKey.p8"
          printf '%s' "$APP_STORE_CONNECT_API_KEY" > "$KEY_FILE"
          APP_STORE_CONNECT_KEY_FILE="$KEY_FILE" just appstore
          rm "$KEY_FILE"

      # Create the store version, wait for the build to process, attach it,
      # set What's New from the tag annotation (if any), and submit for
      # review. The version auto-releases after approval.
      - name: Submit for App Review
        env:
          APP_STORE_CONNECT_API_KEY: ${{ secrets.APP_STORE_CONNECT_API_KEY }}
          APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          export APP_VERSION="${GITHUB_REF_NAME#v}"
          export APP_BUILD="$(git rev-list --count HEAD)"
          KEY_FILE="$RUNNER_TEMP/AuthKey.p8"
          printf '%s' "$APP_STORE_CONNECT_API_KEY" > "$KEY_FILE"
          export RELEASE_NOTES="$(git tag -l --format='%(contents)' "$GITHUB_REF_NAME" | head -c 3900)"
          ASC_KEY_FILE="$KEY_FILE" \
          ASC_KEY_ID="$APP_STORE_CONNECT_KEY_ID" \
          ASC_ISSUER_ID="$APP_STORE_CONNECT_ISSUER_ID" \
            swift scripts/appstore-release.swift --submit
          rm "$KEY_FILE"

  release:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install tools
        run: brew install xcodegen create-dmg just

      - name: Import Developer ID certificate
        env:
          CERT_P12: ${{ secrets.MACOS_CERTIFICATE_P12 }}
          CERT_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
        run: |
          KEYCHAIN="$RUNNER_TEMP/build.keychain"
          KEYCHAIN_PASSWORD=$(uuidgen)
          echo "$CERT_P12" | base64 --decode > "$RUNNER_TEMP/cert.p12"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security import "$RUNNER_TEMP/cert.p12" -k "$KEYCHAIN" \
            -P "$CERT_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S "apple-tool:,apple:" \
            -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security list-keychains -d user -s "$KEYCHAIN" login.keychain
          rm "$RUNNER_TEMP/cert.p12"

      - name: Build signed DMG
        run: |
          export APP_VERSION="${GITHUB_REF_NAME#v}"
          export APP_BUILD="$(git rev-list --count HEAD)"
          just pkg

      - name: Notarize
        env:
          APP_STORE_CONNECT_API_KEY: ${{ secrets.APP_STORE_CONNECT_API_KEY }}
          APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          KEY_FILE="$RUNNER_TEMP/AuthKey.p8"
          printf '%s' "$APP_STORE_CONNECT_API_KEY" > "$KEY_FILE"
          xcrun notarytool submit build/Grumble.dmg \
            --key "$KEY_FILE" \
            --key-id "$APP_STORE_CONNECT_KEY_ID" \
            --issuer "$APP_STORE_CONNECT_ISSUER_ID" \
            --wait
          rm "$KEY_FILE"
          xcrun stapler staple build/Grumble.dmg
          xcrun stapler staple build/pkg/dmg/Grumble.app

      - name: Package Sparkle update and regenerate appcast
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          mkdir -p build/updates
          ditto -c -k --keepParent build/pkg/dmg/Grumble.app \
            "build/updates/Grumble-$VERSION.zip"
          KEY_FILE="$RUNNER_TEMP/sparkle_key"
          printf '%s' "$SPARKLE_PRIVATE_KEY" > "$KEY_FILE"
          build/SourcePackages/artifacts/sparkle/Sparkle/bin/generate_appcast \
            build/updates \
            --ed-key-file "$KEY_FILE" \
            --download-url-prefix "https://github.com/$GITHUB_REPOSITORY/releases/download/$GITHUB_REF_NAME/"
          rm "$KEY_FILE"
          cp build/updates/appcast.xml "$RUNNER_TEMP/appcast.xml"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          cp build/Grumble.dmg "build/Grumble-$VERSION.dmg"
          # The unversioned DMG gives the website a stable
          # releases/latest/download/Grumble.dmg URL.
          gh release create "$GITHUB_REF_NAME" \
            "build/Grumble-$VERSION.dmg" \
            "build/Grumble.dmg" \
            "build/updates/Grumble-$VERSION.zip" \
            --title "Grumble $VERSION" \
            --generate-notes

      # Fill the cask template with this release's version + .dmg sha256 and
      # push it to the tap, so `brew install --cask fcjr/fcjr/grumble` tracks
      # the release. The token comes from the releaser GitHub App, which is
      # installed on fcjr/homebrew-fcjr.
      - name: Mint tap token
        id: tap-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.RELEASER_APP_ID }}
          private-key: ${{ secrets.RELEASER_APP_PRIVATE_KEY }}
          owner: fcjr
          repositories: homebrew-fcjr

      - name: Publish the Homebrew cask
        env:
          TAP_TOKEN: ${{ steps.tap-token.outputs.token }}
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          sha="$(shasum -a 256 "build/Grumble-$VERSION.dmg" | awk '{print $1}')"
          git clone "https://x-access-token:${TAP_TOKEN}@github.com/fcjr/homebrew-fcjr.git" "$RUNNER_TEMP/tap"
          mkdir -p "$RUNNER_TEMP/tap/Casks"
          sed -e "s/VERSION_PLACEHOLDER/$VERSION/" -e "s/SHA256_PLACEHOLDER/$sha/" \
            homebrew/grumble.rb > "$RUNNER_TEMP/tap/Casks/grumble.rb"
          cd "$RUNNER_TEMP/tap"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Casks/grumble.rb
          git diff --cached --quiet || git commit -m "grumble $VERSION"
          git push

      - name: Commit updated appcast and nix pin to main
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          # SRI hash of the same zip the release uploaded, for the nix flake.
          sha="sha256-$(openssl dgst -sha256 -binary "build/updates/Grumble-$VERSION.zip" | base64)"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git checkout -f -B main origin/main
          cp "$RUNNER_TEMP/appcast.xml" apps/web/public/desktop/darwin/appcast.xml
          printf '{ "version": "%s", "sha256": "%s" }\n' "$VERSION" "$sha" > nix/version.json
          git add apps/web/public/desktop/darwin/appcast.xml nix/version.json
          git diff --cached --quiet || git commit -m "Update appcast and nix pin for $GITHUB_REF_NAME"
          git push origin main

      - name: Deploy website
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          pnpm install --frozen-lockfile
          pnpm run deploy
